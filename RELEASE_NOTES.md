## v3.1.3 — 修复卫星自动更新 + 新增风云四号 + 修复失效卫星

### 修复问题

- **修复卫星影像自动刷新不更新** — 卫星影像（CIRA 瓦片）此前没有缓存 TTL，自动刷新时若时间码未变则永远返回旧缓存，壁纸看似不更新。现增加 8 分钟缓存有效期，过期后自动重新下载，确保自动刷新获取最新影像
- **修复 GOES-16 卫星失效** — CIRA RAMMB-Slider 已停止提供 GOES-16 全盘彩色影像（返回 404），替换为新一代 GOES-19（2025 年发射，全盘彩色影像正常可用）
- **修复 scheduler 卫星刷新未传 force=True** — 调度器调用 `fetch_satellite_image` 时未强制刷新，结合无 TTL 导致旧图长期缓存。现 scheduler 强制传 `force=True` 确保每次调度都获取最新影像

### 新增功能

- **新增中国风云四号（FY-4B）卫星影像** — 接入国家卫星气象中心（NSMC）官方数据源，提供 10992×11912 超高分辨率真彩色全圆盘图像，每 15 分钟更新，国内访问无需 VPN
  - 数据源：`https://img.nsmc.org.cn/CLOUDIMAGE/FY4B/AGRI/GCLR/FY4B_DISK_GCLR.JPG`
  - 支持分辨率：1080p / 2K / 4K
  - 自动刷新间隔：默认 15 分钟

### 卫星列表（6 颗）

| 卫星 | 区域 | 状态 |
|------|------|------|
| GOES-19 (美洲) | 美洲 | ✅ 正常 |
| GOES-18 (美洲西) | 美洲西 | ✅ 正常 |
| Himawari-8 (亚太) | 亚太 | ✅ 正常 |
| GK2A (韩国) | 亚太 | ✅ 正常 |
| Meteosat 0度 (欧洲/非洲) | 欧洲/非洲 | ✅ 正常 |
| Meteosat-9 (印度洋) | 印度洋 | ✅ 正常 |
| 风云四号 FY-4B (中国) | 亚太 | ✅ 新增，国内直连 |

---

## v3.1.2 — 修复自动刷新后影像无法更新 + 水印时间不准确

### 修复问题

- **修复 SDO 影像永不自动更新** — SDO 影像缓存使用固定文件名且不检查有效期，导致自动刷新时永远返回旧缓存影像，桌面壁纸无法更新。现增加 10 分钟缓存有效期，过期后自动重新下载最新影像
- **修复水印时间不准确（卫星 / SDO）** — 水印上的"拍摄时间"此前用的是刷新那一刻的运行时系统时间，而非影像的实际拍摄时间，导致用户看到"水印时间一直在变、但影像内容却没变"。现改为读取影像的真实拍摄/更新时间（卫星取 CIRA 时间戳、SDO 取服务器 Last-Modified），水印时间与影像内容严格对应

### 使用说明

- 更新后，卫星 / SDO 壁纸水印将显示影像的真实拍摄时间
- SDO 影像将在缓存超过 10 分钟后自动获取最新图

---

## v3.1.1 — 修复退出残留 + 新增壁纸位置/缩放调节

### 修复问题

- **删除水印上的系统时间显示** — 默认不再在水印角标中显示系统时间，界面更简洁
- **退出后同步关闭后台程序，零残留** — 重构退出流程：关闭软件时统一停止调度器线程、关闭 Flask 服务器、退出系统托盘、销毁窗口，并强制终止所有残留子进程，确保电脑上不残留后台进程

### 新增功能

- **图片位置与缩放调节** — 在「设置」中可调节壁纸图片的缩放比例（50%~200%）与水平/垂直位置（-400~400px），配合「居中」壁纸样式使用，手动模式下即时生效

### 使用说明

- 在「设置 → 壁纸样式」选择「居中」后，「图片位置与缩放」滑块即可生效
- 调节后点击「保存」，再对任意图片点击「设为壁纸」即可应用新的位置/缩放

---

## v3.1.0 — 修复「自动刷新后自动设为壁纸」机制

### 新增/修复功能

- **修复自动刷新后无法自动设为壁纸** — 应用启动即在后端启动常驻调度器，按当前选中的数据源定时自动刷新并设为壁纸，不再依赖前端窗口打开
- **「自动刷新」与「自动设为壁纸」独立开关** — 可分别控制卫星、SDO、APOD 三种数据源是否自动刷新、刷新后是否自动设为壁纸
- **真实倒计时同步** — 前端倒计时改为基于后端下次刷新时间，与调度器实际刷新时机一致
- **切换数据源即时生效** — 界面切换 APOD / 卫星 / SDO 时同步写入后端配置，调度器自动跟随
- **修复首次启动调度器崩溃** — 首次运行时 `last_update` 为空导致的调度器报错已修复

### 涉及数据源

| 数据源 | 自动刷新间隔 | 自动设壁纸开关 |
|--------|--------------|----------------|
| NASA APOD | 每日（可设时间） | `apod_auto_set_wallpaper` |
| 卫星影像 | 可配置（默认 10 分钟） | `sat_auto_set_wallpaper` |
| SDO 太阳观测 | 可配置（默认 60 分钟） | `sdo_auto_set_wallpaper` |

---
