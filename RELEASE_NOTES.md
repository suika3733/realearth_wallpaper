## v3.1.1 — 修复退出残留 + 新增壁纸位置/缩放调节

### 修复问题

- **删除水印上的系统时间显示** — 默认不再在水印角标中显示系统时间，界面更简洁
- **退出后同步关闭后台程序，零残留** — 重构退出流程：关闭软件时统一停止调度器线程、关闭 Flask 服务器、退出系统托盘、销毁窗口，并强制终止所有残留子进程，确保电脑上不残留后台进程

### 新增功能

- **图片位置与缩放调节** — 在「设置」中可调节壁纸图片的缩放比例（50%~200%）与水平/垂直位置（-400~400px），配合「居中」壁纸样式使用，手动模式下即时生效

### 使用说明

- 在「设置 → 壁纸样式」选择「居中」后，「图片位置与缩放」滑块即可生效
- 调节后点击「保存」，再对任意图片点击「设为壁纸」即可应用新的位置/缩放

---

## v3.1.0 — 修复「自动刷新后自动设为壁纸」机制

### 新增/修复功能

- **修复自动刷新后无法自动设为壁纸** — 应用启动即在后端启动常驻调度器，按当前选中的数据源定时自动刷新并设为壁纸，不再依赖前端窗口打开
- **「自动刷新」与「自动设为壁纸」独立开关** — 可分别控制卫星、SDO、APOD 三种数据源是否自动刷新、刷新后是否自动设为壁纸
- **真实倒计时同步** — 前端倒计时改为基于后端下次刷新时间，与调度器实际刷新时机一致
- **切换数据源即时生效** — 界面切换 APOD / 卫星 / SDO 时同步写入后端配置，调度器自动跟随
- **修复首次启动调度器崩溃** — 首次运行时 `last_update` 为空导致的调度器报错已修复

### 涉及数据源

| 数据源 | 自动刷新间隔 | 自动设壁纸开关 |
|--------|--------------|----------------|
| NASA APOD | 每日（可设时间） | `apod_auto_set_wallpaper` |
| 卫星影像 | 可配置（默认 10 分钟） | `sat_auto_set_wallpaper` |
| SDO 太阳观测 | 可配置（默认 60 分钟） | `sdo_auto_set_wallpaper` |

---
